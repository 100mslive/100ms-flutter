class MeetingController {
  final String roomId;

  MeetingController({required this.roomId});

  void startMeeting() {
    // HMSConfig hmsConfig = HMSConfig(userId: userId,
    //     roomId: roomId,
    //     authToken: authToken,
    //     endpoint: Constant.getTokenURL)
  }
}
